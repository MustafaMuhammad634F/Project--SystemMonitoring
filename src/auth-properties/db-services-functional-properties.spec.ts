import { DbServicesFunctionalProperties } from './db-services-functional-properties';

describe('DbServicesAuthSP', () => {
  it('should create an instance', () => {
    expect(new DbServicesFunctionalProperties()).toBeTruthy();
  });
});
